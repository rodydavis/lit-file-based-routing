{
  "name": "lit-file-based-router",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "npm run ts && npm run generate",
    "generate": "npm run ts && node dist/generator/index.js",
    "ts": "tsc"
  },
  "dependencies": {
    "lit": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  }
}